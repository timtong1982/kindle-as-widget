import * as React from 'React';

const app = (): React.ReactElement => {
  const createElement = (): JSX.Element => {
    return <div>Test content aafs2fssffsff2</div>;
  };
  return createElement();
};

export { app };
